specVersion: "v1"
serviceId: "smartbinwebsite"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "smartbinwebsite-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
